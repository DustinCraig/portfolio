import { createTheme } from "@mui/material/styles";

export const ACCENT_COLOR = "#3eb4c1";

export const theme = createTheme({
  palette: {
    mode: "dark",
  },
  typography: {
    fontFamily: "Consolas, monaco, monospace",
  },
});
